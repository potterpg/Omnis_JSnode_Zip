On evClick ## Event Parameters - pRow( Itemreference )
	Do FileOps.$getfilename(lcFilePath,'Please select the file to add:','',sys(10))
	If len(lcFilePath)>0
		Do ioZip.$AddFiles(lcFilePath)
	End If